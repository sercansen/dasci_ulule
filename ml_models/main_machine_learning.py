

def machine_learning(cat_name: str, dict_df: dict) -> str:
    if cat_name == "tout":
        # On utilise les CSVs généraux comme "clean_data_pre_covid.csv"
        pass
    else:
        # On utilise les CSVs spéficiques rangés dans un dossier bien précis : /data/data_cat_covid
        pass
    return ""
